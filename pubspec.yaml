name: toxic_tracker
description: 今天鸽了吗 - 一款极简作死记录应用
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  shared_preferences: ^2.2.2
  intl: ^0.18.1
  image_picker: ^1.0.7
  share_plus: ^7.2.2
  supabase_flutter: ^2.6.0
  purchases_flutter: ^8.1.0
  connectivity_plus: ^6.0.3
  path_provider: ^2.1.2
  flutter_local_notifications: ^17.2.3
  timezone: ^0.9.4
  home_widget: ^0.7.0
  flutter_tts: ^4.0.2
  audioplayers: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true
